# This file is APPENDED into the project's gradle/libs.versions.toml by
# scripts/apply-overlay.sh. Sections are merged: existing [versions], [libraries],
# [plugins], [bundles] entries are preserved; these get added (existing keys skipped).
#
# IMPORTANT: this overlay does NOT re-pin the toolchain (Kotlin, AGP, Compose,
# AndroidX Lifecycle, Coroutines). kmp.new's generated catalog already owns those keys
# (`kotlin`, `agp`, `composeMultiplatform`, `androidx-lifecycle`, `kotlinx-coroutines`),
# and the build-logic plugin entries below reference THOSE keys — so build-logic and the
# app modules share one toolchain version (no "plugin on classpath with different
# version" failure) and the overlay stays drift-proof across future kmp.new bumps.
#
# Use `/kmp-forge-bump-stack` to refresh versions.

[versions]
# --- locked stack ---
orbitMvi = "9.0.0"
koin = "4.0.4"
koinAndroidx = "4.0.4"
coil = "3.1.0"
ktor = "3.1.2"
kermit = "2.0.5"
kotlinxDatetime = "0.6.2"
kotlinxSerialization = "1.8.1"
androidxNavigation3 = "1.1.1"
androidxDatastore = "1.1.7"
sqldelight = "2.1.0"
turbine = "1.2.1"
detekt = "1.23.7"
spotless = "7.0.2"
ktlint = "1.5.0"
kover = "0.9.8"
store = "5.1.0-alpha05"
kotlinResult = "2.3.1"

# NOTE: kotlin / agp / composeMultiplatform / androidx-lifecycle / kotlinx-coroutines
# are intentionally NOT declared here — they come from kmp.new's catalog. See header.

[libraries]
# Orbit MVI
orbit-core = { module = "org.orbit-mvi:orbit-core", version.ref = "orbitMvi" }
orbit-viewmodel = { module = "org.orbit-mvi:orbit-viewmodel", version.ref = "orbitMvi" }
orbit-compose = { module = "org.orbit-mvi:orbit-compose", version.ref = "orbitMvi" }
orbit-test = { module = "org.orbit-mvi:orbit-test", version.ref = "orbitMvi" }

# Koin
koin-core = { module = "io.insert-koin:koin-core", version.ref = "koin" }
koin-compose = { module = "io.insert-koin:koin-compose", version.ref = "koin" }
koin-compose-viewmodel = { module = "io.insert-koin:koin-compose-viewmodel", version.ref = "koin" }
koin-android = { module = "io.insert-koin:koin-android", version.ref = "koin" }

# Coil 3
coil-compose = { module = "io.coil-kt.coil3:coil-compose", version.ref = "coil" }
coil-network-ktor = { module = "io.coil-kt.coil3:coil-network-ktor3", version.ref = "coil" }

# Ktor (opt-in)
ktor-client-core = { module = "io.ktor:ktor-client-core", version.ref = "ktor" }
ktor-client-content-negotiation = { module = "io.ktor:ktor-client-content-negotiation", version.ref = "ktor" }
ktor-serialization-kotlinx-json = { module = "io.ktor:ktor-serialization-kotlinx-json", version.ref = "ktor" }
ktor-client-okhttp = { module = "io.ktor:ktor-client-okhttp", version.ref = "ktor" }
ktor-client-darwin = { module = "io.ktor:ktor-client-darwin", version.ref = "ktor" }
ktor-client-java = { module = "io.ktor:ktor-client-java", version.ref = "ktor" }
ktor-client-js = { module = "io.ktor:ktor-client-js", version.ref = "ktor" }

# Kermit
kermit = { module = "co.touchlab:kermit", version.ref = "kermit" }

# kotlin-result — two-param Result<V, E> for Result + DomainError error handling.
# Maven Central coordinate is "com.michael-bull.kotlin-result"; the Kotlin import
# package is "com.github.michaelbull.result.*" (Ok/Err/onSuccess/onFailure/fold/...).
kotlin-result = { module = "com.michael-bull.kotlin-result:kotlin-result", version.ref = "kotlinResult" }
kotlin-result-coroutines = { module = "com.michael-bull.kotlin-result:kotlin-result-coroutines", version.ref = "kotlinResult" }

# KotlinX
kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "kotlinx-coroutines" }
kotlinx-coroutines-test = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-test", version.ref = "kotlinx-coroutines" }
kotlinx-datetime = { module = "org.jetbrains.kotlinx:kotlinx-datetime", version.ref = "kotlinxDatetime" }
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinxSerialization" }

# AndroidX
# lifecycle-viewmodel-compose is already in kmp.new's catalog as `androidx-lifecycle-viewmodelCompose`
# (same artifact) — referenced via libs.androidx.lifecycle.viewmodelCompose. Only the non-compose
# viewmodel artifact is unique to this overlay.
androidx-lifecycle-viewmodel = { module = "org.jetbrains.androidx.lifecycle:lifecycle-viewmodel", version.ref = "androidx-lifecycle" }
# Nav 3: runtime stays on Google's androidx.navigation3 (a true KMP artifact — iOS/macOS/js/wasm);
# the UI port lives under org.jetbrains.androidx.navigation3 because Google's navigation3-ui is
# Android/JVM-only. The UI port depends on the matching Google runtime, so one version ref drives both.
androidx-navigation3-runtime = { module = "androidx.navigation3:navigation3-runtime", version.ref = "androidxNavigation3" }
androidx-navigation3-ui = { module = "org.jetbrains.androidx.navigation3:navigation3-ui", version.ref = "androidxNavigation3" }
androidx-datastore-core = { module = "androidx.datastore:datastore-core", version.ref = "androidxDatastore" }
androidx-datastore-preferences-core = { module = "androidx.datastore:datastore-preferences-core", version.ref = "androidxDatastore" }

# SQLDelight (opt-in)
sqldelight-runtime = { module = "app.cash.sqldelight:runtime", version.ref = "sqldelight" }
sqldelight-coroutines = { module = "app.cash.sqldelight:coroutines-extensions", version.ref = "sqldelight" }
sqldelight-android-driver = { module = "app.cash.sqldelight:android-driver", version.ref = "sqldelight" }
sqldelight-native-driver = { module = "app.cash.sqldelight:native-driver", version.ref = "sqldelight" }
sqldelight-sqlite-driver = { module = "app.cash.sqldelight:sqlite-driver", version.ref = "sqldelight" }
sqldelight-web-worker-driver = { module = "app.cash.sqldelight:web-worker-driver", version.ref = "sqldelight" }

# Test — kotlin-test comes from kmp.new's catalog (version.ref = "kotlin").
turbine = { module = "app.cash.turbine:turbine", version.ref = "turbine" }

# Store (MobileNativeFoundation) — opt-in for offline-first multi-source repos
store = { module = "org.mobilenativefoundation.store:store5", version.ref = "store" }

# --- gradle plugins consumed by build-logic ---
# Toolchain plugins reference kmp.new's catalog keys (kotlin / agp / composeMultiplatform)
# so build-logic compiles against the SAME versions the app modules use — no classpath clash.
kotlin-gradle-plugin = { module = "org.jetbrains.kotlin:kotlin-gradle-plugin", version.ref = "kotlin" }
android-gradle-plugin = { module = "com.android.tools.build:gradle", version.ref = "agp" }
compose-gradle-plugin = { module = "org.jetbrains.compose:compose-gradle-plugin", version.ref = "composeMultiplatform" }
compose-compiler-gradle-plugin = { module = "org.jetbrains.kotlin:compose-compiler-gradle-plugin", version.ref = "kotlin" }
detekt-gradle-plugin = { module = "io.gitlab.arturbosch.detekt:detekt-gradle-plugin", version.ref = "detekt" }
spotless-gradle-plugin = { module = "com.diffplug.spotless:spotless-plugin-gradle", version.ref = "spotless" }
kover-gradle-plugin = { module = "org.jetbrains.kotlinx:kover-gradle-plugin", version.ref = "kover" }

[plugins]
# composeMultiplatform + composeCompiler come from kmp.new's catalog — only plugins
# kmp.new does NOT define are added here.
kotlinx-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
detekt = { id = "io.gitlab.arturbosch.detekt", version.ref = "detekt" }
spotless = { id = "com.diffplug.spotless", version.ref = "spotless" }
kover = { id = "org.jetbrains.kotlinx.kover", version.ref = "kover" }
sqldelight = { id = "app.cash.sqldelight", version.ref = "sqldelight" }
