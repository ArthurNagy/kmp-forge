package ${BASE_PACKAGE}.domain

import org.koin.dsl.module

/**
 * Koin module for :domain. Add use cases here as the project grows.
 * Repository interfaces are bound in :data's module to their implementations.
 */
val domainModule = module {
    // factoryOf(::GetSomethingUseCase)
    // factoryOf(::DoSomethingUseCase)
}
