package ${BASE_PACKAGE}.feature.${FEATURE_NAME}

import org.koin.core.module.dsl.viewModelOf
import org.koin.dsl.module

val ${FEATURE_NAME}Module = module {
    viewModelOf(::${FEATURE_NAME_PASCAL}ViewModel)
}
