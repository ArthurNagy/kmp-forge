package ${BASE_PACKAGE}.ui

import androidx.compose.material3.Typography

/**
 * Typography tokens. Wraps M3 Typography defaults; override per-style as the design system grows.
 * Composables ALWAYS read font sizes from MaterialTheme.typography — never hardcode .sp.
 */
object AppType {
    val material: Typography = Typography()
}
