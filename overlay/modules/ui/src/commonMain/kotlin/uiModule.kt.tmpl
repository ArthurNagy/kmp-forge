package ${BASE_PACKAGE}.ui

import org.koin.dsl.module

/**
 * Koin module for :ui. Currently empty — design tokens are objects, no DI needed.
 * Add bindings here if reusable Composables grow stateful collaborators (e.g. an image loader factory).
 */
val uiModule = module {}
